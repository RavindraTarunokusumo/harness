# Iteration Archive

This directory stores completed iteration items migrated from `TODO.md`.

`TODO.md` is the primary source of truth for active work. Once an item is complete, move it into the appropriate file here.


- Move the full completed item (including sub-items) from `TODO.md`.
- Add session title and PR link
- Tag each item and sub-items with their merge/commit IDs
